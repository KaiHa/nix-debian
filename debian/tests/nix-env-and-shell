#!/usr/bin/make -f


all:
	nix-channel --add https://nixos.org/channels/nixpkgs-unstable
	nix-channel -vvvvv --update
	nix-shell -vvvvv -p nix-du --run "nix-du --help" '<nixpkgs>'
	nix-env -vvvvv -iA nixpkgs.nix-du
	nix-du --help
