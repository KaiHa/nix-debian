sudo: true
os: linux
dist: bionic
language: cpp

script:
  - cat /etc/issue
  - ./ci test-stretch

addons:
  apt:
    packages:
    - autopkgtest
    - cowbuilder
    - debhelper
    - debian-archive-keyring
    - devscripts
    - dh-autoreconf
    - fakeroot
    - libvirt-bin
    - libwww-perl
    - lxc
    - pdebuild
